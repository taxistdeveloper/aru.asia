# Фото должны отдаваться напрямую и не переписываться роутером
<IfModule mod_rewrite.c>
RewriteEngine Off
</IfModule>

<IfModule mod_authz_core.c>
Require all granted
</IfModule>

<IfModule mod_access_compat.c>
Order allow,deny
Allow from all
</IfModule>

